1. 当 margin-top、padding-top 的值是百分比时，分别是如何计算的？ D
- A. 相对父级元素的 height，相对自身的 width
- B. 相对最近父级块级元素的 height，相对自身的 height
- C. 相对父级元素的 width，相对自身的 width
- D. 相对最近父级块级元素的 width，相对最近父级块级元素的 width

2. calc() 的运算规则
- 使用加减乘除四则运算
- 可以混合各种单位进行计算
- 有 +，- 时，其前后必须要有空格
- 有 *，/ 时，其前后可以灭有空格