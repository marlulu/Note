## 函数

>  filter是过滤出符合条件的元素。  
every是检测数组是否所有元素都满足条件。  
some是检测数组是否有满足条件的元素。  
map是遍历数组，且对每个元素进行操作。

全局函数
- escape( )、eval( )、isFinite()、isNaN( )、parseFloat( )、parseInt( )、unescape( )

- eval 将会返回对最后一个表达式的求值结果


- defineProperty为对象设置属性后，该属性的writable、configurable以及 enumerable 默认为false 
